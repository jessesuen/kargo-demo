# Kargo IAM

This `policy.json` file contain the permissions necessary to give Kargo to:
* monitor ECR warehouse
* Run terraform promotions

Additionally contains permissions to push to the`lambda-demo/lambda-app` ECR repository

It is not the permissions necessary for the workload app.